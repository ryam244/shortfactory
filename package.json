{
  "name": "shortfactory",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "studio": "pnpm --filter @shortfactory/video studio",
    "render:sample": "pnpm --filter @shortfactory/video render:sample"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
